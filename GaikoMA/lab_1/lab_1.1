#include <iostream>
#include <conio.h>
using namespace std;
int main() {
	int number1, number2;
	char operation;
	const int vihod = 27;
	do {
		cout << "Enter  to 1 number" << endl;
		cin >> number1;
		cout << "Enter  to 2 number" << endl;
		cin >> number2;
		cout << "Enter to the operation" << endl;
		cin >> operation;
		switch (operation) {
		case '+' :
			cout << number1 + number2 << endl;
			break;
		case '-':
			cout << number1 - number2 << endl;
			break;
		case '*':
			cout << number1 * number2 << endl;
			break;
		case '/':
			if (number2 != 0) {
				cout << number1 + number2;
				break;
			}
			else {
				cout << "Error. division to 0" << endl;
				break;
			}
		default:
			cout << "Error. Vvod he och" << endl;
			break;
		}
		cout << "Enter to 'esc' to exit" << endl;	

	} while (_getch() != vihod);
	return 0;
}
